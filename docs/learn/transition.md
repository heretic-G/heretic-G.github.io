
[文档](https://www.w3.org/TR/css-transitions-1/#starting)

具体可以看文档 只是遇到了 了解记录下 没怎么看...

额 实际就是转换效果在未完成的时候中断 这里不能使用原有的参数去实现效果 会导致不一致 官方实现了一个中断计算的逻辑 他上面还写了会导致中断后的反向时间缩短 但是看下面说并没有完全解决2个状态间的转换

博客 这里主要是看他transition 第一个设置上了 但是因为层叠样式机制 hover的transition上来了  这里他生效 然后展示的是他的正向  hover 取消 层叠样式 原有的transition生效 这里是他的反向

反向涉及一个反向的开始值还有一个反向缩短因子  [0, 1]
完成的反向缩短因子是1


