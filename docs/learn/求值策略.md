值传递

    传递的是一个副本 实参在函数内部所有操作都不会影响外部

引用传递

    传递的是一个变量别名 那这时候别名其实就是相当于变量 所有的操作都是在操作原有变量一样

共享传递 
    
    这是后提的一个概念 主要是部分语言在行为上出现交叉现象 比如js的引用类型 这里其实不是别名的作用但是在堆中的操作又会反馈到原有数据
    在赋值又不会同步外部变量像是值传递那样

严格求值 
    
    立即求值 在函数之前求值

非严格求值 

    也就是惰性求值 在使用的时候才会求值

求值顺序的话这里就按照语言定义的设置就好了 