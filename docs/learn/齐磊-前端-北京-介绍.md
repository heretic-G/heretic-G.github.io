老师，针对面试的项目内容做了一些总结，希望能帮忙看下 有哪些内容或者方向上 还可能被提问，在难度上或者内容还可以在扩展
还有哪些可以话术上的优化。我觉得我的项目可能难度比较低，希望能尽量在扩充下

个人介绍：

我叫齐磊 工作时间5年半 在2家公司工作过，第二家工作3年半 开始负责saas在线教育开发，后来做saas管理端开发，
去年公司转型，整体教育线舍弃后，主要是在负责在线办公的管理端和官网，因为转型导致目前我负责的内容业务量很少，
而且感觉自己也到了一个瓶颈，希望到更大的平台去工作、成长

短板：
有一些情况会是兴趣驱动，导致很多内容碎片化，没有整体性。

再做一遍做的更好：
埋点，数据上报我认为对于项目更重要，他能够告诉我，我做的一个事情的价值是正向还是负向。在做项目中其实可以去调动别的组 去完成一些你的想法


saas融合管理系统项目介绍：(被提问难点 我也更倾向这个项目)
这个项目对于我可能让我不是在关注在一个业务需求上，而是去做一些对于整个项目有效益的事情和思考 去推动typescript使用率
推动切换http2 推动HSTS，还有就是使用node去基于json生成一些基础的编辑页和列表页
因为立项的目的公司希望能够整合之前的几个saas管理端，希望能快速迭代满足新的需求。所以这里我和产品还有设计 以文档形式敲定下来一些需求和设计约束，
比如表单结构，校验、字段过滤，一些基础需求页面的样式，这样我们可以积累一些通用的组件去增加开发效率。这也促成很多页面大体是一致
所以当时对编辑创建表单表单 列表页这种页面用node 去生成模板 再去开发，使用一段后感觉我能不能json配置他直接去生成，
因为以编辑页面来看只是字段名 key 校验规则 和请求的API，不过这里实际效果没有想像的好，后来有做一个单独的json编辑工具去生成这个json也没有提高很多效率
后来也有考虑可视化的方案，因为可视化感觉去生成json更方便 尤其是这里可能能够解放我，不过这里的问题是api和字段需要配置的人去看文档，
对于校验这些也需要了解具体的含义，还有就是我现在很多工作都是在项目内去做的 他可能还有更多的前置工作去需要完成，这个成本在沟通后他们觉得过于的高

上节课
疫情后的一个saas在线教育产品，目标期望是toC，支持用户简单的创课，收集信息，分销，商品推送，和一些关于信息收集、上课情况的展示，
信息收集主要是用户在管理端去创建一个问卷，然后在用户买课的时候去填写，问卷支持输入，输入校验，单选 多选，多选其他，在管理端生成json后在用户端根据json去做展示

分销卡片最开始是我们提供的固定的几个卡片背景和固定的信息展示， 后面为了更多样化 支持用户上传图片，自定义头像、二维码和课程简介的位置，也可以用户自己去制作好上传图片，

同视官网

公司信息展示和信息收集还有被邀请和创空间的的几个表单页面，
最初是为了seo所以直接定的就是多页的项目，html用的ejs 然后会根据js文件夹下去创建多入口 在plugin中根据ejs去创建多个html chunks里面添加对应的页面js、common(公共依赖的通用的方法和函数)、vender(node_modules里的依赖包)
这个项目更多是在做优化，基于lighthouse的意见调整，
cdn错误的资源域名切换，document绑订error 对于css image直接修改域名回填 js等load后需要顺序创建 默认async是true这里需要修改下 在触发下domcontentLoad 和onload事件

基于事件模型封装好的按钮loading，点击捕获冒泡中会标志捕获API，在请求上如果存在标志 回收起来收录在对用dom上 用了自增id当做标志
如果处于捕获的API返回的then是自定义的then模拟promise 在then传入的方法中会继续标志状态捕获后面串行的API保持继续loading

还有最近在做的提前首屏按钮依赖API数据时的可交互时间，最开始就是按钮loading，后面我想可以先操作，所以做了一个独立的promise API请求完直接调独立的resolve，然后后面依赖数据的直接去依赖独立的promise就可以
不过又改了一版，其实大体逻辑没有变动，我是把数据改成Proxy 劫持value属性的get 当不存在会去请求数据 存在直接返回数据使用直接await data 后续使用就可以









