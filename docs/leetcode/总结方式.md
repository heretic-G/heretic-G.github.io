


不知道长度的随机算法 蓄水池算法

固定长度的随机算法   洗牌算法

链表在一般逻辑下可以实现个头结构之后在计算 方便使用 (不然很多时候需要判断各种逻辑 )

循环数组的流程可以看看能不能去扩大倍数 直接操作 如果有限循环的逻辑能够替换无限循环的逻辑 有限就是一个解


#### 图的几种方式



数据 一般就是邻接表和邻接矩阵
    
    表的优势省空间
    矩阵的优势就是能直接知道谁和谁相连吗

二分图判断

    二分图实际就是一条边的2个点在2边 能不能所有的点都满足这个特性 
    一般都是邻接表 根据一个走下去 这时候 dfs完直接另一个开始
    染色...第一次写 人都写裂开了 想岔了 挨个没有颜色就标记 DFS下去就好了

有环图无环图的判断

    主要是区分有向还是无向图
    有向图用拓扑排序

查并集

    看了下 感觉很有意思的逻辑在实现中
    压缩路径
    平衡树高度

拓扑排序

    拓扑排序首先处理所有的入度 按照入度0去减去入度 直到所有的为空 如果最后全部都处理完不存在环 生成的数组就是线性的排序

最经典的最小生成树

prim 算法真的很简单 首先是多余的边去掉权高的 循环的去掉 随机选一个点 每次都判断最小的 取就完事了...需要找个题来写一写
越是逻辑简答的 可能实现就会出各种问题

单源最短路径


#### 二分查找 

二分不光是那种明面的顺序搜索 有一些可以从线性答案中去用二分加快查找 

最主要的就是理解 判断条件和朝向哪边收窄 千万不要收窄收着收着 搜索的内容没了... 

常规我们习惯mid向下取整这时候要小心 向下取整一定要保证left有条件去上升 不然就会死循环 

#### 链表

翻转链表 中间 倒数第几个 这种都是指向对应的 挨个处理就好了 这里一般做一个pre指向head 更好操作一般都

判断环

判断环就是快慢指针 这里快2慢1相遇就是存在环

环入口

存在环的时候 慢等于head 然后fast slow 挨个移动再相遇就是入口

环长度就是第一次相遇 还是快2慢1在走直到第二次相遇

链表排序 归并排序



