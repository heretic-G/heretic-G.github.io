1. flex:1 这个是由哪些属性组合，分别代表什么意思？
    三个属性 放大 缩小 宽度 
    放大默认0 不放大 
    缩小1 根据比例缩小 
    宽度auto 
    
2. 204 301 302 304 400 401 403  404，你认识其中哪些状态码？
    301 永久重定向 302临时重定向 304 资源未变更
    400 参数错误请求 404 没找到
3. 箭头函数可以被被当做构造函数new实例化吗？ 箭头函数可以调用call改变this指向吗
    不能 没有内部[[constructor]] 实际是被改变了 但是箭头函数内部thismode是词法的所以这里直接去取了外部执行上下文的this来使用
4. fiber对于React的意义是什么（React技术栈作答）或者 说下Vue的keep-alive组件原理（Vue技术栈作答）
    我是react fiber能够让render流程暂停恢复，支撑后面的异步渲染流程 
    keep-alive 这里会内部存在一个element 基于key去做cache  只会对包裹的第一个生效 内部还处理了生命周期的调用
5. 观察者模式和发布订阅模式的区别是什么，你还知道哪些设计模式？
    观察者是在被观察者上 然后去触发的 发布订阅解耦了触发事件和监听事件的双方
   策略 单例 工厂 享元 状态
6. 什么是浏览器内核包含哪些，常见的有哪些？
   
7. 宏任务和微任务的区别？
   在event loop中task一次执行一个job会循环清空
8. 自己配置过前端自动化打包工具吗？webpack官网有认真学习过吗？
   目前只会webpack 配置过 基本的操作都会 基本概念也还可以
9. 有参与过新人培养、团队技术沉淀（比如团队公众号、分享、开源）么？
    自己有github 但是只是自己的一些文章记录
10. 有参与过开发提效、业务保障（比如线上监控）、性能优化的经验么？
    node去生成模板页面 基于json去动态生成页面(只是一些简单的创建和list页面)
    性能优化的话大文件上传webworker优化
