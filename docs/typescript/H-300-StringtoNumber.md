```typescript


type Map = {
    '0': []
    '1': [1]
    '2': [...Map['1'], 1]
    '3': [...Map['2'], 1]
    '4': [...Map['3'], 1]
    '5': [...Map['4'], 1]
    '6': [...Map['5'], 1]
    '7': [...Map['6'], 1]
    '8': [...Map['7'], 1]
    '9': [...Map['8'], 1]
}
type Make10Array<T extends any[]> = [
    ...T,
    ...T,
    ...T,
    ...T,
    ...T,
    ...T,
    ...T,
    ...T,
    ...T,
    ...T
]
type ToNumber<
    S extends string,
    L extends any[] = []
    > = S extends `${infer F}${infer R}`
    ? ToNumber<R, [...Make10Array<L>, ...(F extends keyof Map ? Map[F] : never)]>
    : L['length']


```

这题没有想明白 或者说没有明白怎么处理大数 

看了下居然一部分答案是直接使用每次增加一个数组长度来判断是不是符合...

有点为了过测来写... 

上面记录的是一种 我觉得比较清晰的逻辑  一个number 

他是从左开始生成N个 然后每次乘十 + m

比如271

第一轮是 2

第二轮是 2 * 10 + 7

第三轮是 27 * 10 + 1

思路也不算是难 就是要绕过那个弯 我一直是想从尾开始开始一步一步怎么进位 或者我是想能不能

第一轮 1

第二轮 1 + 7 * 10

第三轮 71 + 2 * 100 

也许存在这种方式 但是我目前没写出来 
