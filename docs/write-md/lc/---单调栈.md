单调栈 

栈

单调递增或者递减 区分是递增还是递减式按照出栈后的顺序决定的 一般都用数组去实现 所以这里其实会和index是反的 
但是仔细想其实这个才是对的 你把他立起来他如果是递增还是递减 其实就是从指向栈顶到栈底那递增就是栈顶到栈底是递增


时间复杂度 其实比我想象的低 他算是0(n)的其实仔细想也对 因为每个元素只会进行一次出栈入栈操作 

单调栈其实就是再约束一步的栈 在操作的时候就是如果符合单调的方向 直接推入 如果不符合 就出栈到符合在入栈

哨兵法
